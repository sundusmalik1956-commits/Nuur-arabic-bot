# -*- coding: utf-8 -*-
"""
config.py
إعدادات وثوابت المشروع التي ليست أسرارًا حساسة (التوكن والمفاتيح تبقى في .env فقط).
"""

import os

# معرّف قروب إعلانات الإنجاز — البوت مضاف فيه كمشرف
ACHIEVEMENT_GROUP_ID = int(os.environ.get("ACHIEVEMENT_GROUP_ID", "-1003785748588"))

# الأوقات المقترحة لاختيار الطالب (يمكن تعديلها بلا أثر على بقية النظام)
AVAILABLE_TIMES = [
    "06:00", "07:00", "08:00", "09:00", "10:00", "11:00",
    "12:00", "13:00", "14:00", "15:00", "16:00", "17:00",
    "18:00", "19:00", "20:00", "21:00", "22:00", "23:00",
]

# رابط بوت اختبار تحديد المستوى (بوت منفصل عن نور بوت حاليًا، مجرد رابط للطالب)
PLACEMENT_TEST_BOT_LINK = "https://t.me/Nurarabictestbot"

# معرّفات تيليجرام الرقمية (user_id) للمشرفين المخوَّلين باستخدام أوامر إدارية مثل /export
ADMIN_USER_IDS = {
    int(uid.strip())
    for uid in os.environ.get("ADMIN_USER_IDS", "8840764246").split(",")
    if uid.strip()
}

# المنفذ الذي يستمع عليه سيرفر الصحة الداخلي (مطلوب فقط عند التشغيل كـ Render Web Service).
# Render يمرر PORT تلقائيًا كمتغير بيئة؛ القيمة الافتراضية هنا للتشغيل المحلي فقط.
HEALTH_CHECK_PORT = int(os.environ.get("PORT", "10000"))
